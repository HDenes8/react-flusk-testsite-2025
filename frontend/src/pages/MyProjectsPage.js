import React from 'react';
import MainPage from './MainPage';

const MyProjectsPage = () => {
  return <MainPage defaultRoleFilter="owner" showFilterDropdown={false} />;
};

export default MyProjectsPage;